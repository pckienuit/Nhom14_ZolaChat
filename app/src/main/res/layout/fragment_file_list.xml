<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:orientation="vertical"
    android:background="@color/white">

    <!-- Filter Chips Section -->
    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:orientation="vertical"
        android:paddingStart="8dp"
        android:paddingEnd="8dp"
        android:paddingTop="12dp"
        android:paddingBottom="12dp"
        android:background="@color/white"
        android:elevation="2dp">

        <!-- Filter Chips in Horizontal Scroll -->
        <HorizontalScrollView
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:fillViewport="false"
            android:scrollbars="none">

            <com.google.android.material.chip.ChipGroup
                android:id="@+id/filterChipGroup"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                app:singleSelection="false"
                app:singleLine="true"
                app:chipSpacing="8dp"
                app:chipSpacingHorizontal="8dp">

                <!-- Media Type Filter Chip (visible only for MEDIA category) -->
                <com.google.android.material.chip.Chip
                    android:id="@+id/mediaFilterChip"
                    style="@style/Widget.MaterialComponents.Chip.Choice"
                    android:layout_width="wrap_content"
                    android:layout_height="wrap_content"
                    android:text="Tất cả"
                    android:checkable="false"
                    android:visibility="gone"
                    app:ensureMinTouchTargetSize="false"
                    app:chipIcon="@drawable/ic_filter"
                    app:closeIconVisible="false" />

                <!-- File Type Filter Chip (visible only for FILES category) -->
                <com.google.android.material.chip.Chip
                    android:id="@+id/fileTypeFilterChip"
                    style="@style/Widget.MaterialComponents.Chip.Choice"
                    android:layout_width="wrap_content"
                    android:layout_height="wrap_content"
                    android:text="Tất cả"
                    android:checkable="false"
                    android:visibility="gone"
                    app:ensureMinTouchTargetSize="false"
                    app:chipIcon="@drawable/ic_filter"
                    app:closeIconVisible="false" />

                <com.google.android.material.chip.Chip
                    android:id="@+id/senderFilterChip"
                    style="@style/Widget.MaterialComponents.Chip.Filter"
                    android:layout_width="wrap_content"
                    android:layout_height="wrap_content"
                    android:text="@string/filter_by_sender"
                    android:checkable="false"
                    app:ensureMinTouchTargetSize="false"
                    app:chipIcon="@drawable/ic_person" />

                <com.google.android.material.chip.Chip
                    android:id="@+id/dateFilterChip"
                    style="@style/Widget.MaterialComponents.Chip.Filter"
                    android:layout_width="wrap_content"
                    android:layout_height="wrap_content"
                    android:text="@string/filter_by_date"
                    android:checkable="false"
                    app:ensureMinTouchTargetSize="false"
                    app:chipIcon="@drawable/ic_calendar" />

                <com.google.android.material.chip.Chip
                    android:id="@+id/domainFilterChip"
                    style="@style/Widget.MaterialComponents.Chip.Filter"
                    android:layout_width="wrap_content"
                    android:layout_height="wrap_content"
                    android:text="@string/filter_by_domain"
                    android:visibility="gone"
                    android:checkable="false"
                    app:ensureMinTouchTargetSize="false"
                    app:chipIcon="@drawable/ic_link" />

                <com.google.android.material.chip.Chip
                    android:id="@+id/clearFiltersChip"
                    android:layout_width="wrap_content"
                    android:layout_height="wrap_content"
                    android:visibility="gone"
                    android:text=""
                    android:contentDescription="Xóa bộ lọc"
                    app:chipIcon="@drawable/ic_close"
                    app:chipIconTint="#F44336"
                    app:chipBackgroundColor="#FFEBEE"
                    app:chipStrokeColor="#F44336"
                    app:chipStrokeWidth="1dp"
                    app:chipIconSize="16dp"
                    app:chipMinTouchTargetSize="0dp"
                    app:chipStartPadding="0dp"
                    app:chipEndPadding="0dp"
                    app:iconStartPadding="6dp"
                    app:iconEndPadding="6dp"
                    app:textStartPadding="0dp"
                    app:textEndPadding="0dp"
                    app:chipCornerRadius="50dp"
                    android:clickable="true"
                    android:focusable="true" />

            </com.google.android.material.chip.ChipGroup>

        </HorizontalScrollView>

    </LinearLayout>

    <!-- Content Section with SwipeRefresh -->
    <FrameLayout
        android:layout_width="match_parent"
        android:layout_height="0dp"
        android:layout_weight="1">

        <!-- SwipeRefreshLayout for pull-to-refresh -->
        <androidx.swiperefreshlayout.widget.SwipeRefreshLayout
            android:id="@+id/swipeRefresh"
            android:layout_width="match_parent"
            android:layout_height="match_parent">

            <androidx.recyclerview.widget.RecyclerView
                android:id="@+id/recyclerView"
                android:layout_width="match_parent"
                android:layout_height="match_parent"
                android:clipToPadding="false"
                android:padding="8dp" />

        </androidx.swiperefreshlayout.widget.SwipeRefreshLayout>

        <!-- Empty state -->
        <LinearLayout
            android:id="@+id/emptyStateLayout"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_gravity="center"
            android:gravity="center"
            android:orientation="vertical"
            android:visibility="gone">

            <ImageView
                android:id="@+id/emptyStateIcon"
                android:layout_width="80dp"
                android:layout_height="80dp"
                android:src="@drawable/ic_file"
                android:alpha="0.3"
                android:contentDescription="@string/no_files" />

            <TextView
                android:id="@+id/emptyStateText"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:layout_marginTop="16dp"
                android:text="@string/no_results"
                android:textColor="@android:color/darker_gray"
                android:textSize="16sp" />

        </LinearLayout>

        <!-- Loading indicator -->
        <ProgressBar
            android:id="@+id/progressBar"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_gravity="center"
            android:visibility="gone" />

    </FrameLayout>

</LinearLayout>
