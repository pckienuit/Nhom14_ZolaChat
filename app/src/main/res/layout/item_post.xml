<?xml version="1.0" encoding="utf-8"?>
<androidx.cardview.widget.CardView xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:layout_margin="8dp"
    app:cardCornerRadius="8dp"
    app:cardElevation="2dp"
    app:cardBackgroundColor="#FFFFFF">

    <androidx.constraintlayout.widget.ConstraintLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:paddingBottom="12dp">

        <!-- 1. Avatar User (ID: imgAvatarPost) -->
        <de.hdodenhof.circleimageview.CircleImageView
            android:id="@+id/imgAvatarPost"
            android:layout_width="48dp"
            android:layout_height="48dp"
            android:layout_margin="12dp"
            android:src="@drawable/ic_launcher_background"
            app:civ_border_color="#FF000000"
            app:civ_border_width="0dp"
            app:layout_constraintStart_toStartOf="parent"
            app:layout_constraintTop_toTopOf="parent" />

        <!-- 2. Tên User (ID: tvUserNamePost) -->
        <TextView
            android:id="@+id/tvUserNamePost"
            android:layout_width="0dp"
            android:layout_height="wrap_content"
            android:layout_marginStart="12dp"
            android:layout_marginTop="14dp"
            android:layout_marginEnd="8dp"
            android:text="Tên người dùng"
            android:textColor="#000000"
            android:textStyle="bold"
            android:textSize="16sp"
            app:layout_constraintEnd_toEndOf="parent"
            app:layout_constraintStart_toEndOf="@id/imgAvatarPost"
            app:layout_constraintTop_toTopOf="parent" />

        <!-- 3. Thời gian đăng (ID: tvTimePost) -->
        <TextView
            android:id="@+id/tvTimePost"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:text="1 giờ trước"
            android:textColor="#757575"
            android:textSize="12sp"
            app:layout_constraintStart_toStartOf="@id/tvUserNamePost"
            app:layout_constraintTop_toBottomOf="@id/tvUserNamePost" />

        <!-- 4. Nội dung bài viết - Text (ID: tvContentPost) -->
        <TextView
            android:id="@+id/tvContentPost"
            android:layout_width="0dp"
            android:layout_height="wrap_content"
            android:layout_marginTop="12dp"
            android:layout_marginHorizontal="12dp"
            android:text="Nội dung bài viết sẽ hiển thị ở đây..."
            android:textColor="#000000"
            android:textSize="15sp"
            app:layout_constraintEnd_toEndOf="parent"
            app:layout_constraintStart_toStartOf="parent"
            app:layout_constraintTop_toBottomOf="@id/imgAvatarPost" />

        <!-- 5. Ảnh bài viết (ID: imgContentPost) -->
        <ImageView
            android:id="@+id/imgContentPost"
            android:layout_width="match_parent"
            android:layout_height="250dp"
            android:layout_marginTop="12dp"
            android:scaleType="centerCrop"
            android:src="@drawable/ic_launcher_background"
            android:visibility="visible"
            app:layout_constraintTop_toBottomOf="@id/tvContentPost"
            tools:layout_editor_absoluteX="0dp" />

        <!-- Đường kẻ phân cách -->
        <View
            android:id="@+id/divider"
            android:layout_width="match_parent"
            android:layout_height="1dp"
            android:layout_marginTop="12dp"
            android:background="#E0E0E0"
            app:layout_constraintTop_toBottomOf="@id/imgContentPost" />

        <!-- KHU VỰC TƯƠNG TÁC -->

        <!-- 6. Nút Like (ID: btnLike) -->
        <ImageView
            android:id="@+id/btnLike"
            android:layout_width="24dp"
            android:layout_height="24dp"
            android:layout_marginTop="12dp"
            android:layout_marginStart="16dp"
            android:src="@drawable/ic_like"
            app:layout_constraintStart_toStartOf="parent"
            app:layout_constraintTop_toBottomOf="@id/divider" />

        <!-- 7. Số lượng Like (ID: tvLikeCount) -->
        <TextView
            android:id="@+id/tvLikeCount"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_marginStart="8dp"
            android:text="0"
            android:gravity="center_vertical"
            app:layout_constraintBottom_toBottomOf="@id/btnLike"
            app:layout_constraintStart_toEndOf="@id/btnLike"
            app:layout_constraintTop_toTopOf="@id/btnLike" />

        <!-- 8. Nút Bình luận (ID: btnComment) -->
        <ImageView
            android:id="@+id/btnComment"
            android:layout_width="24dp"
            android:layout_height="24dp"
            android:layout_marginStart="32dp"
            android:src="@drawable/ic_comment"
            app:layout_constraintBottom_toBottomOf="@id/btnLike"
            app:layout_constraintStart_toEndOf="@id/tvLikeCount"
            app:layout_constraintTop_toTopOf="@id/btnLike" />

        <!-- 9. Số lượng Bình luận (ID: tvCommentCount) -->
        <TextView
            android:id="@+id/tvCommentCount"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_marginStart="8dp"
            android:text="Bình luận"
            android:gravity="center_vertical"
            app:layout_constraintBottom_toBottomOf="@id/btnComment"
            app:layout_constraintStart_toEndOf="@id/btnComment"
            app:layout_constraintTop_toTopOf="@id/btnComment" />

    </androidx.constraintlayout.widget.ConstraintLayout>
</androidx.cardview.widget.CardView>